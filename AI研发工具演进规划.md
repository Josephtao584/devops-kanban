# AI 研发工具演进规划

## 主线:驱动力的三次迁移

```
   阶段一 人驱动  ──门槛A:配置数据化──▶  阶段二 数据驱动  ──门槛B:反馈闭环化──▶  阶段三 系统驱动
   Human-Config                        Data-Driven                          Self-Evolving
   人是决策者                           数据是决策者                          系统是决策者
   AI 是执行引擎                        人做纠偏                              人只定义业务目标
```

### 门槛 A(一→二)配置数据化

把人手写的 Prompt/工具/技能/策略,在每次执行启动时冻结成不可变、可回放的快照。

```
   阶段一: 运行时人工挂载           门槛A: 启动时冻结快照
   ┌─────────────────┐           ┌──────────────────────────┐
   │ 人每次手动配:    │           │  不可变执行快照 Snapshot   │
   │  · Prompt       │  ───▶     │  ┌──────────────────────┐ │
   │  · 挂哪些工具    │           │  │ Prompt + 工具引用      │ │
   │  · 挂哪些技能    │           │  │ 技能引用 + 上下文      │ │
   │  · 重试/确认策略 │           │  │ 策略 + 变量            │ │
   └─────────────────┘           │  └──────────────────────┘ │
        易漂移 不可比               │  一次执行 = 一份快照        │
                                  └──────────────────────────┘
                                     │可回放 │可隔离 │可审计
```

解锁可回放(评估飞轮成立)、可隔离(敢自动改配置)、可审计(自进化有实验记录)。

### 门槛 B(二→三)反馈闭环化

把人从回路里拿掉——Trace 不再只给人看,而是系统自己分析、自动改自己、自动验证回滚。

```
   阶段二: 人在回路 (Human-in-the-loop)
   Trace ─▶ 看板/报告 ─▶ 人分析 ─▶ 人改配置 ─▶ 下次执行
                          ▲ 瓶颈在这里(人)

   阶段三: 人在回路之上 (Human-on-the-loop)
   Trace ─▶ 系统分析 ─▶ 自动改 Prompt/工具/Harness ─▶ 验证 ─▶ 上线
                                                       │
            人只在"定义目标"和"异常兜底"时介入 ◀────────┘
```

前提是门槛 A 的可回放能力 + 自动验证能力(改完能自己测、自己回滚)。

## 阶段一:AI 与人协同(把能力接进来)

```
┌─────────────────────────────────────────────────────────┐
│  人类 Tech Lead                                           │
│   │  写 Prompt   配权限   配环境   做 Code Review          │
│   ▼                                                       │
│  ┌────────┐   API/CLI    ┌──────────┐                     │
│  │   AI   │ ───────────▶ │ 传统工具  │ Jira/Git/Jenkins    │
│  │ Agent  │   被动调用    │ (保留UI)  │                     │
│  └───┬────┘              └──────────┘                     │
│      ▼ 在隔离环境里执行单任务                                │
│  ┌──────────────┐                                         │
│  │  基础沙箱     │  单 Agent · 启动慢 · 并发争抢             │
│  └──────────────┘                                         │
└─────────────────────────────────────────────────────────┘
```

- **IT 工具**:API/CLI 接入,人工配权限,工具保留 UI、AI 被动调用。
- **Harness 工程**:专家手工写 Prompt 模板与策略,无评估。
- **Agent Runtime**:基础隔离沙箱,单 Agent,启动慢、并发争抢。
- **知识库**:静态 RAG + AST 代码图谱,AI 被动查询。
- **合格线**:AI 能在隔离环境独立完成被明确描述的任务,产出能过人工 Review。

## 阶段二:经验驱动进阶(建评估飞轮)

```
              ┌──────────── 评估飞轮 ────────────┐
              │                                  │
   ┌──────────▼─────────┐          ┌─────────────┴────────┐
   │  采集执行 Trace      │          │  半自动优化            │
   │  · 人类修改 Diff     │          │  · 识别低效 Prompt     │
   │  · Review 意见       │  ───▶    │  · 推荐工具组合        │
   │  · CI 失败日志       │          │  · Auto-Prompting     │
   │  · 每次尝试状态      │          │                       │
   └────────────────────┘          └─────────────┬────────┘
              ▲                                   │
              └───────────── 数据回流 ─────────────┘

   Runtime: 弹性池·秒级拉起·多Agent并发·断点续传·DAG编排
   ┌───┐ ┌───┐ ┌───┐        A ─▶ B ─▶ D
   │Pod│ │Pod│ │Pod│             │     ▲
   └───┘ └───┘ └───┘             └─ C ─┘  (有向无环图)
```

- **IT 工具**:全面 MCP 化退居幕后,反馈日志结构化采集,自动推荐/组合工具链。
- **Harness 工程**:自动收集 Diff/Review/CI 失败建评估飞轮,识别低效 Prompt(Auto-Prompting)。
- **Agent Runtime**:弹性沙箱池秒级拉起,多 Agent 并发,断点续传,DAG 动态编排。
- **知识库**:索引"人解决问题的过程"(Trace),跨 Agent 上下文共享,风险预测。
- **合格线**:系统能指出"哪个 Prompt 低效、换哪个工具组合更优",并在并发与复杂依赖编排下稳定运行。
- **最易卡点**:只做了采集端就以为完成——差的是反馈端(把 Trace 喂回去),见门槛 B。

## 阶段三:AI 自进化(把反馈闭环回去)

```
┌─────────────────────────────────────────────────────────────┐
│                    研发认知大脑 (闭环)                          │
│  ┌─────────────┐    ┌─────────────┐    ┌──────────────┐        │
│  │ Self-Tooling │◀──▶│ Self-Harness│◀──▶│ 群体智能 Swarm│        │
│  │ 缺能力就自己  │    │ 自生成       │    │ Agent 自主协商 │        │
│  │ 写/测/注册工具│    │ 自淘汰       │    │ 动态组队       │        │
│  └──────┬──────┘    └──────┬──────┘    └──────┬───────┘        │
│         └──────────────────┼──────────────────┘                │
│                            ▼                                   │
│              ┌──────────────────────────┐                     │
│              │ 知识库: 遗忘 + 更新机制     │                     │
│              │ 主动巡检技术债             │                     │
│              │ 修复经验 ─▶ 沉淀为新知识    │                     │
│              └──────────────────────────┘                     │
│                            ▲                                   │
│                 人: 只定义业务目标 + 异常兜底                     │
└─────────────────────────────────────────────────────────────┘
```

- **IT 工具**:Self-Tooling,发现能力缺失时自主写/测/注册新工具。
- **Harness 工程**:自生成/自淘汰,随环境自动重构,低效自动归档,新策略自动验证。
- **Agent Runtime**:群体智能 Swarm,Agent 自主协商组队,全局资源统筹,故障自愈,跨沙箱状态迁移。
- **知识库**:具备遗忘/更新机制,主动巡检技术债,修复经验自动沉淀为新知识。
- **合格线**:给一个业务目标,系统自拆解、自组队、自执行、失败自修、经验自沉淀,人只在边界与异常时介入。

## 总表:四组件 × 三阶段

```
┌──────────┬────────────────┬────────────────┬────────────────┐
│          │ 阶段一 协同     │ 阶段二 经验驱动 │ 阶段三 自进化   │
│          │ (接能力)       │ (建飞轮)       │ (闭环)         │
├──────────┼────────────────┼────────────────┼────────────────┤
│ IT 工具  │ API/CLI 接入    │ MCP化+结构采集  │ Self-Tooling    │
│          │ 人工配权限      │ 自动推荐组合    │ 自写自测自注册   │
├──────────┼────────────────┼────────────────┼────────────────┤
│ Harness  │ 手工写 Prompt   │ 评估飞轮        │ 自生成/自淘汰   │
│          │ 无评估          │ Auto-Prompting  │ 自动验证新策略   │
├──────────┼────────────────┼────────────────┼────────────────┤
│ Runtime  │ 基础沙箱        │ 弹性池+并发     │ 群体智能 Swarm  │
│          │ 单 Agent        │ DAG编排+续传    │ 故障自愈+迁移   │
├──────────┼────────────────┼────────────────┼────────────────┤
│ 知识库   │ 静态RAG+AST     │ Trace记忆+共享  │ 遗忘/更新+巡检  │
│          │ 被动查询        │ 风险预测        │ 经验自动沉淀     │
└──────────┴────────────────┴────────────────┴────────────────┘
      ↑                ↑门槛A 配置数据化  ↑门槛B 反馈闭环化
   人驱动            数据驱动           系统驱动
```

## 落地优先级

1. **先做配置数据化(门槛 A)**:不可变执行快照是一切自主能力的前提,不能跳过。
2. **补反馈端而非只做采集端(门槛 B)**:飞轮的价值在把数据喂回去,自动优化 Harness、自动入知识库。
3. **打通执行 Trace 与知识库**:让 Agent 不只查文档,而是查"上次这个任务怎么失败又怎么修好的"。

## 三句话收尾

1. **阶段一→二的本质是"配置数据化"**:没有不可变、可回放的执行快照,数据驱动无从谈起。
2. **阶段二的引擎是"执行 Trace 闭环"**:采集只是入场券,真正的飞轮在反馈端。
3. **阶段三的门票是"可回放实验 + 自动验证"**:让每次执行都成为可对比、可审计、可自动验证的实验,系统才敢自己改自己。
