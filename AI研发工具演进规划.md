# AI 研发工具演进规划

## 主线:驱动力的三次迁移

```
   阶段一 人驱动  ──门槛A:配置数据化──▶  阶段二 数据驱动  ──门槛B:反馈闭环化──▶  阶段三 系统驱动
   Human-Config                        Data-Driven                          Self-Evolving
   人是决策者                           数据是决策者                          系统是决策者
   AI 是执行引擎                        人做纠偏                              人只定义业务目标
```

- **门槛 A(一→二)配置数据化**:把人手写的 Prompt/工具/技能/策略,在每次执行启动时冻结成不可变、可回放的快照。解锁可回放(评估飞轮成立)、可隔离(敢自动改配置)、可审计(自进化有实验记录)。
- **门槛 B(二→三)反馈闭环化**:把人从回路里拿掉——Trace 不再只给人看,而是系统自己分析、自动改自己、自动验证回滚。前提是门槛 A 的可回放能力 + 自动验证能力。

## 四组件 × 三阶段:每个阶段该做什么

| 组件 | 阶段一 协同(接能力) | 阶段二 经验驱动(建飞轮) | 阶段三 自进化(闭环) |
|---|---|---|---|
| **IT 工具** | API/CLI 接入,人工配权限,工具保留 UI、AI 被动调用 | 全面 MCP 化退居幕后,反馈日志结构化采集,自动推荐/组合工具链 | Self-Tooling:发现能力缺失时自主写/测/注册新工具 |
| **Harness 工程** | 专家手工写 Prompt 模板与策略,无评估 | 自动收集 Diff/Review/CI 失败建评估飞轮,识别低效 Prompt(Auto-Prompting) | 自生成/自淘汰:随环境自动重构,低效自动归档,新策略自动验证 |
| **Agent Runtime** | 基础隔离沙箱,单 Agent,启动慢、并发争抢 | 弹性沙箱池秒级拉起,多 Agent 并发,断点续传,DAG 动态编排 | 群体智能 Swarm:Agent 自主协商组队,全局资源统筹,故障自愈,跨沙箱状态迁移 |
| **知识库** | 静态 RAG + AST 代码图谱,AI 被动查询 | 索引"人解决问题的过程"(Trace),跨 Agent 上下文共享,风险预测 | 具备遗忘/更新机制,主动巡检技术债,修复经验自动沉淀为新知识 |

## 各阶段的合格线

- **阶段一**:AI 能在隔离环境独立完成被明确描述的任务,产出能过人工 Review。
- **阶段二**:系统能指出"哪个 Prompt 低效、换哪个工具组合更优",并在并发与复杂依赖编排下稳定运行。
- **阶段三**:给一个业务目标,系统自拆解、自组队、自执行、失败自修、经验自沉淀,人只在边界与异常时介入。

## 落地优先级

1. **先做配置数据化(门槛 A)**:不可变执行快照是一切自主能力的前提,不能跳过。
2. **补反馈端而非只做采集端(门槛 B)**:多数团队卡在"采集了 Trace 却没闭环"——飞轮的价值在把数据喂回去,自动优化 Harness、自动入知识库。
3. **打通执行 Trace 与知识库**:让 Agent 不只查文档,而是查"上次这个任务怎么失败又怎么修好的"。

## 三句话收尾

1. **阶段一→二的本质是"配置数据化"**:没有不可变、可回放的执行快照,数据驱动无从谈起。
2. **阶段二的引擎是"执行 Trace 闭环"**:采集只是入场券,真正的飞轮在反馈端。
3. **阶段三的门票是"可回放实验 + 自动验证"**:让每次执行都成为可对比、可审计、可自动验证的实验,系统才敢自己改自己。
